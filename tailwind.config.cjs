const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    screens: {
      sm: "890px",
    },
  },

  // Extra config
  mode: "jit", // Experimental setting
};

module.exports = config;
